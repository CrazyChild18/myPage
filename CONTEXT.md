# Voyage Planner Context

## Domain Language

- **行程区域**: 一段旅行的主要地图语境，分为 `domestic` 和 `overseas`。国内行程使用高德，境外行程使用 Google。
- **地图供应商**: 地图展示、地点检索、地址解析和路线能力的来源，目前为 `amap` 或 `google`。
- **坐标系**: 地点在数据层保存和渲染时采用的坐标约定。跨平台数据默认保存 WGS84；高德渲染时转换为 GCJ-02。
- **地点来源**: 用户选择地点时返回的供应商身份和地点 ID，例如 Google Place ID 或高德 POI ID。
- **路线来源**: 路线几何和时长来自哪个供应商。当前阶段先保留现有直线/大圆渲染，后续按地图供应商接入正式路线。

## Architecture Notes

- 地图能力应该集中在地图供应商 Module 后面。页面只关心当前行程的地图供应商，不直接知道 Google/高德请求细节。
- 地点检索只在用户点击搜索时触发，结果进入缓存，避免个人项目里因为输入联想造成不必要计费。
- 数据模型需要保存地点来源和坐标系，让 Web、Android、Mac 后续可以共用同一份行程数据。
